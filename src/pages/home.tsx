import { TopSection } from '../containers/home/top-section.tsx';
import Layout from '../layout';

const HomePage = () => {
  return (
    <Layout>
      <TopSection />
    </Layout>
  );
};
export default HomePage;
